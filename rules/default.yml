rules:
  - id: VL-PRIV-001
    title: Logging request body
    description: Request body logging may expose PII
    severity: high
    confidence: 0.7
    globs: ["**/*.rs"]
    pattern: '(?i)println!\(.*request.*body'
    axes:
      privacy: 1.0
